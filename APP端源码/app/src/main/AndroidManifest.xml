<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a14532.mymovie">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".data.local.GlobalData" >
        <activity android:name=".subject.SubjectActivity"/>
        <activity android:name=".login.LoginActivity"/>
        <activity android:name=".register.RegisterActivity"/>
        <activity android:name=".movielist.MovieListActivity"/>
        <activity android:name=".search.SearchActivity"/>
        <activity android:name=".typesearch.TypeSearchActivity"/>
        <activity android:name=".photo.PhotoActivity"/>
        <activity android:name=".root.RootActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".usercount.UserCountActivity" />
        <activity android:name=".moviecount.MovieCountActivity" />
    </application>

</manifest>